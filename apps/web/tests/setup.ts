// The storage tests exercise the IndexedDB layer in a node environment, which
// has no IndexedDB. `fake-indexeddb/auto` installs a spec-compliant in-memory
// implementation on globalThis.
import 'fake-indexeddb/auto';
